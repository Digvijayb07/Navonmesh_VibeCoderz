import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Navonmesh — Trust-Enabled Agricultural Exchange Platform",
  description:
    "A premium agricultural marketplace connecting farmers, buyers, and logistics providers with AI-powered price discovery, trust scoring, and secure escrow transactions.",
  keywords: [
    "agriculture",
    "marketplace",
    "farming",
    "trust",
    "exchange",
    "agritech",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${poppins.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
